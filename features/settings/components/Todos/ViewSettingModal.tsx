import React from 'react';

interface ViewSettingModalProps {
  isOpen: boolean;
  onClose: () => void;
}

const ViewSettingModal = ({ isOpen, onClose }: ViewSettingModalProps) => {
  return <div></div>;
};

export default ViewSettingModal;
